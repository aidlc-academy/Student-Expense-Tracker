/**
 * Scaffold smoke test — verifies the test infrastructure itself works.
 * This file can be removed once real unit tests are in place.
 */
import { describe, it, expect } from "@jest/globals";

describe("scaffold", () => {
  it("jest is configured for ESM", () => {
    expect(true).toBe(true);
  });
});
